import VetPlaceholderPage from "./VetPlaceholderPage";

// Layout/nav/routing pass only — real dashboard (stat cards, today's
// appointments, emergency requests, quick actions) is next feature chat.
export default function VetDashboard() {
  return (
    <VetPlaceholderPage
      title="Dashboard"
      description="Your vet dashboard overview will live here."
    />
  );
}